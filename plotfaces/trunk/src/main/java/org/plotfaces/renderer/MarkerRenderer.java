package org.plotfaces.renderer;

/**
 * Draws the marker that indicates a data point on a chart. Used by
 * {@code ChartSeries} and {@code Highlighter}.
 *
 * @author Graham Smith
 */
public interface MarkerRenderer extends Renderer {
}
